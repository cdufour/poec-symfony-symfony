<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Player</title>
  </head>
  <body>
    <p>Infos sur le joueur {{player.name}}</p>
    <p>Numéro: {{player.num}}</p>
    {% if player.substitute %}
    <p>Remplaçant</p>
    {% else %}
    <p>Titulaire</p>
    {% endif %}
    <img src="{{player.photo}}" alt="{{player.name}}">
  </body>
</html>
